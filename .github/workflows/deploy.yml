name: CI
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: install
        run: npm install
      - name: deploy site updates
        run: npm run deploy
