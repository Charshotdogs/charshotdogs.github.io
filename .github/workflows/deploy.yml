name: CI
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: download gh-pages
        run: npm install gh-pages
      - name: deploy site updates
        run: npm run deploy
